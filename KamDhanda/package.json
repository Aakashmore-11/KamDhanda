{
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "production": "npm run install-all && npm run build && npm start"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
